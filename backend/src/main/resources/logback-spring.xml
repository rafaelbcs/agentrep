<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Dev: log legível no console -->
    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="br.com.agentrep" level="DEBUG"/>
    </springProfile>

    <!-- Prod: log JSON estruturado para ingestão (Grafana Loki / ELK) -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>walletAddress</includeMdcKeyName>
                <customFields>{"app":"agentrep","env":"prod"}</customFields>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
        <logger name="br.com.agentrep" level="INFO"/>
        <!-- Silencia logs verbosos de libs externas -->
        <logger name="org.web3j" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

</configuration>
